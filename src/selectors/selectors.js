export const searchTermSelector = (state) => state.search.searchTerm;

export const searchResultsSelector = (state) => state.search.results;

export const selectedIssueSelector = (state) => state.search.selectedIssue;
